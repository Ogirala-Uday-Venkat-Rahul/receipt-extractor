# Path to the quantized GGUF model the API serves.
# After training + merging + quantizing, drop the .gguf here or set this to a
# Hugging Face Hub path handled by the loader.
MODEL_PATH=models/receipt-extractor-q4.gguf

# Threads for llama.cpp inference (match the host's CPU count).
LLAMA_THREADS=4
